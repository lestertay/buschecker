{
  "private": true,
  "name": "buschecker",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "yarn workspace client start",
    "server": "yarn workspace server start",
    "start": "yarn server"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
